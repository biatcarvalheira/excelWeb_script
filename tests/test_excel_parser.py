# Define a list
my_list = ["apple", "banana", "orange"]

# Iterate over each element in the list and count the characters
for item in my_list:
    character_count = len(item)
    print(f"The number of characters in '{item}' is: {character_count}")
